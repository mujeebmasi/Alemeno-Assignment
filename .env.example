# Optional: set for real LLM calls (Gemini free tier)
GEMINI_API_KEY=your_gemini_api_key_here

# gemini | ollama | fallback
LLM_PROVIDER=gemini

# Only used when LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://host.docker.internal:11434
